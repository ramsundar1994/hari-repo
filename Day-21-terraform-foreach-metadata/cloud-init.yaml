#cloud-config

package_update: true

packages:
  - nginx

runcmd:
  - systemctl start nginx
  - systemctl enable nginx
  - echo "<h1>Welcome to Linux Machine and custom data with terrform code </h1>" > /var/www/html/index.html